[tool.poetry]
name = "pip-install-test-20250106"
version = "0.3.0"
description = ""
authors = ["Robertson Brinker <robertsonbrinker@gmail.com>"]
readme = "README.md"
packages = [{include = "calculator", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
