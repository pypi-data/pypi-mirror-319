[build-system]
requires = [
    "setuptools==70.0.0",
    "pybind11>=2.10.0",
    "torch==2.2.0",
    "numpy==1.24.0",
    "scipy",
    "matplotlib",
    "open3d"
]
build-backend = "setuptools.build_meta"

[tool.setuptools]
include-package-data = true

[project]
name = "lidar_aug"
version = "1.0.0"
requires-python = "<3.12"
dependencies = [
    "torch==2.2.0",
    "matplotlib",
    "numpy==1.24.0",
    "open3d"
]

# target-version = "py37"
