from setuptools import setup, find_packages

setup(
    name="temirlan_rules",
    version="1.0.0",
    author="Temirlan",
    description="The wisdom of Temirlan in one package",
    packages=find_packages(),
    python_requires=">=3.6",
)

