[project]
name = "pairasync"
version = "1.0.0"
description = "Pair async code to your sync code"
readme = "README.md"
authors = [
    { name = "Aaron2599" }
]
requires-python = ">=3.7"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "aiohttp>=3.8.6",
]
