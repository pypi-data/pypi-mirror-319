[project]
name = "deknp"
version = "0.1.77"
description = "Extending from npm"
authors = [
    { name = "sanzenwin", email = "sanzenwin@gmail.com" },
]
dependencies = [
    "json5<=1.6.4",
    "svgpathtools<=1.6.1",
    "dektools[dyna]<1.0.0",
    "dekgen<1.0.0",
    "dekmedia[svg]<1.0.0",
]
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "MIT"

[project.scripts]
deknp = "deknp.click.__entry__:main"

[tool.pdm.build]
includes = [
    "deknp",
]

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"
