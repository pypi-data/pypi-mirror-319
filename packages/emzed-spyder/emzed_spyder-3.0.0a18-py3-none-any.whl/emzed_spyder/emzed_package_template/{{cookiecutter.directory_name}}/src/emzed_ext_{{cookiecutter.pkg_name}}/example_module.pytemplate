import os


def compute():
    return 42


def path_to_data_file(file_name):
    here = os.path.dirname(os.path.abspath(__file__))
    return os.path.join(here, "data", file_name)


def add(a, b):
    """this function is not tested within the example test suite and
    thus the coverage report should show the next line in red"""
    return a + b
