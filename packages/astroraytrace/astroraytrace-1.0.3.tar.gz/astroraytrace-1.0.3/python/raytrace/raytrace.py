import os
import numpy

def raytrace():
    pass

# maybe "surface" define the outer boundary of an optical element
# and a separate 3D object has a surface but also an interior
# through which the ray passes.  maybe call these "objects"?
