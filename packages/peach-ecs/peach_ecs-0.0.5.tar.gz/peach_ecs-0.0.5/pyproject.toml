[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "peach-ecs"
version = "0.0.5"
requires-python = ">=3.10"
authors = [
  { name="Peach", email="james@christhall.us" },
]
readme = "README.md"

[project.urls]
Homepage = "https://gitlab.com/peach_experiments/pecs"

[tool.setuptools.packages]
find = {}
