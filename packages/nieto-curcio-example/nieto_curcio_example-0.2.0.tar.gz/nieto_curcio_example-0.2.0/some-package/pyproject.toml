[project]
name = "some-package"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dsm-ll", email = "diogo@liquidloans.io" }
]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
some-package = "some_package:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
