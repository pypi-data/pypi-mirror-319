[project]
name = "APISimplify"
version = "0.0.6"
description = "A simple API toolkit for Python developers"
readme = "README.md"
requires-python = ">=3.6.0"  
authors = [
    { name = "master_wasdondiscord", email = "masterioempo@gmail.com" }
]
license = { file = "LICENSE" }  

dependencies = [
    "requests >=2.25.0",
    "discord >=1.0.0",
    "Pillow >=8.0.0",
    "jsonschema >=3.2.0"
]

[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"
