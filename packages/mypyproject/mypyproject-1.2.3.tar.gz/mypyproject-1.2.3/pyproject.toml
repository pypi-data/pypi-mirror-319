[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[project]
name = "mypyproject"
version = "1.2.3"

[tool.setuptools]  # configuration specific to the `setuptools` build backend.
packages = { find = { where = ["."], include = ["tools", "tools.*"] } }
