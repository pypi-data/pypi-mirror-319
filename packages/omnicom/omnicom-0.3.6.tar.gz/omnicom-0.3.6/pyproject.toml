[build-system]
requires = ["setuptools>=57.0.0","wheel","pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "omnicom"
version = "0.3.6"
authors=[{name = "Andrew Ogundimu"}]
description="A python module for a multisystem (currently only NES) emulator"
