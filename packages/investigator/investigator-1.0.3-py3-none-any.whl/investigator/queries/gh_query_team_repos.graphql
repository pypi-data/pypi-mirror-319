query ($login: String!, $team_slug: String!) {
  organization(login: $login) {
    team(slug: $team_slug) {
      repositories(first: 100) {
        nodes {
          ... on Repository {
            id
            databaseId
            url
            name
            description
            primaryLanguage {
              name
            }
            archivedAt
            defaultBranchRef {
              name
            }
          }
        }
      }
    }
  }
}
