[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ijp"
version = "1.0.0"
description = "An incremental JSON parser for handling JSON data generated by LLMs."
authors = [
    { name = "JSH", email = "jsh6789@gmail.com" }
]
license = { text = "License :: OSI Approved :: The Unlicense (Unlicense)" }
readme = "README.md"
requires-python = ">=3.9"

[project.urls]
"Repository" = "https://github.com/jsh6789/ijp"

