@PACKAGE_INIT@
set(HWMALLOC_MODULE_PATH ${CMAKE_CURRENT_LIST_DIR})
list(APPEND CMAKE_MODULE_PATH ${HWMALLOC_MODULE_PATH})
include(CMakeFindDependencyMacro)
find_dependency(MPI)
find_dependency(HWMALLOC)
if (@OOMPH_WITH_UCX@)
    #set(UCP_LIBRARY     @UCP_LIBRARIES@)
    #set(UCP_INCLUDE_DIR @UCP_INCLUDE_DIRS@)
    find_dependency(UCX)
    if (@OOMPH_UCX_USE_PMI@)
        #set(PMIX_LIBRARY     @PMIX_LIBRARIES@)
        #set(PMIX_INCLUDE_DIR @PMIX_INCLUDE_DIRS@)
        find_dependency(PMIx)
    endif()
endif()
if (@OOMPH_WITH_LIBFABRIC@)
    #set(LIBFABRIC_LIBRARY     @ULIBFABRIC_LIBRARIES@)
    #set(LIBFABRIC_INCLUDE_DIR @ULIBFABRIC_INCLUDE_DIRS@)
    find_dependency(Libfabric)
endif()
include(${CMAKE_CURRENT_LIST_DIR}/oomph-targets.cmake)

