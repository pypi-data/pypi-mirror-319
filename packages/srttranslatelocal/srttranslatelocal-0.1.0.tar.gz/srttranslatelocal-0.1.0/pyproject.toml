[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
[project]
name = "srttranslatelocal"
version = "0.1.0"
keywords = ["translation", "subtitles", "srt", "plex"]
authors = [
  { name="codexhound"},
]
license = {text = "MIT License"}
description = "Translate SRT Locally Using Argos Translate"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
  "translatehtml",
  "argostranslate",
]
[project.urls]
Homepage = "https://github.com/codexhound/srt_translate_local"
Issues = "https://github.com/codexhound/srt_translate_local/issues"
[project.scripts]
translatevideo = "srt_translate_local:srt_translate_local.main"
