[project]
name = "geneva"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
geneva = "geneva:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
