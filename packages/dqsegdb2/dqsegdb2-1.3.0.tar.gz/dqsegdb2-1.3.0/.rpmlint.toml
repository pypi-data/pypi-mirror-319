Filters = [
  # we _want_ to have dqsegdb2 depend explicitly
  # on the same build of python3-dqsegdb
  ".* requires-on-release python3-dqsegdb2",
  # we use tabs for the hacky setup.cfg file
  "\\\\.spec.* mixed-use-of-spaces-and-tabs",
]
