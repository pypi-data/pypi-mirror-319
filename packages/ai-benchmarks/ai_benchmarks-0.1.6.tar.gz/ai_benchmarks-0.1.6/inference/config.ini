;***支持多section***
;endpoint：必须提供一个有效的endpoint
;concurrent_requests：同时并发的请求数
;batch：执行批次
;timeout: 超时时间，默认60
;max_retries： 最大重试次数，默认1
;duration_enabled：启用持续请求的时间限制, 开启后则不执行concurrent_requests
;duration_time：请求持续时间，默认为5秒
;duration_batch: 请求批次
;duratio_interval: 请求间隔
;ip：用户查询机器资源占用
;username：用户名
;password：密码
;max_connections：最大连接数
;max_keepalive_connections:最大保持活动连接数
;model：自动获取,可手动添加
;owned_by：自动获取，可手动添加
;stream = False 流式回复默认关闭
;prompts =  问题, 为空自动匹配默认问题, 格式 [{"id": 1, "prompt": "Output from 1 to 100"}, {"id": 2, "prompt": "List the top 10 most important people in history."},]



;[qwen1.5-14b-ascend]
;endpoint = http://127.0.0.1:8011/v1
;api_key = HS-QA
;concurrent_requests = 2
;batch = 1
;duration_enabled = False
;duration_request = 20
;duration_batch = 5
;ip = 10.102.254.37
;username = root
;password = hengshi@1qazxsw21
;timeout = 120
;max_retries = 0
;max_connections = 1000
;max_keepalive_connections = 200
;model = qwen2.5:7b
;stream = False
;owned_by = auto
;prompts = [
;          {"id": 1, "prompt": "Output from 1 to 100"},
;          {"id": 2, "prompt": "List the top 10 most important people in history."},
;          {"id": 3, "prompt": "Define the term 'artificial intelligence'."},
;          {"id": 4, "prompt": "Count the number of words in this sentence."},
;          {"id": 5, "prompt": "Translate the following sentence into French: 'Hello, how are you?'"},
;          {"id": 6, "prompt": "Detail the process of photosynthesis."}
;          ]

[llama3.1:8b-ascend]
endpoint = http://127.0.0.1:8011/v1
api_key = HS-QA
concurrent_requests = 2
batch = 1
duration_enabled = False
duration_time = 5
duration_batch = 5
duratio_interval = 0.5
ip = 127.0.0.1
username = root
password = hengshi@1qazxsw21
timeout = 120
max_retries = 0
max_connections = 1000
max_keepalive_connections = 200
model = auto
stream = false
owned_by = auto
prompts = [{"id": 1, "prompt": "Output from 1 to 100"}]