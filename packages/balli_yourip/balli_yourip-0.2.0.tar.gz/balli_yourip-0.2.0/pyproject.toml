[project]
name = "balli_yourip"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "BalliAsghar", email = "ballihacker@outlook.com" }]
requires-python = ">=3.13"
dependencies = ["click>=8.1.8", "requests>=2.32.3"]

[project.scripts]
balli_yourip = "app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app"]
