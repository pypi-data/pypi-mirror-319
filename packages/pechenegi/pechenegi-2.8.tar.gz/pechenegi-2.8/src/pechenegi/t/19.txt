В Python можно передавать переменное количество параметров с помощью *args (неименованные) и **kwargs(именованные). Позиционные параметры, находящиеся в списке, передаются через *, а именованные параметры, находящиеся в словаре, — через **.